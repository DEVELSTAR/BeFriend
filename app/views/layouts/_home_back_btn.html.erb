<div class="posts-btns">
  <%= link_to 'Back', :back, class: 'posts-btn' %>
  <%= current_user.name.present? ? current_user.name : current_user.email%>
  <%= link_to 'Home', root_path, class: 'posts-btn' %>
</div>